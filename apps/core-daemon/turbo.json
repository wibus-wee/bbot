{
  "extends": [
    "//"
  ],
  "tasks": {
    "dev": {
      "env": [
        "CORE_API_TOKEN",
        "DATABASE_URL",
        "NODE_ENV",
        "PORT"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env",
        ".env.*"
      ]
    }
  }
}
