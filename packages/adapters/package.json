{
  "name": "@bbot/adapters",
  "private": true,
  "version": "0.0.0",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "diff": "^5.2.0"
  },
  "devDependencies": {
    "@bbot/typescript-config": "workspace:*",
    "@types/diff": "^5.2.0",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
